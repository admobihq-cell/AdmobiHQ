# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This is the **customer** mobile app (`apps/app-mobile`). It has **no Clerk**.
Ops staff mobile remains at `apps/mobile`.
