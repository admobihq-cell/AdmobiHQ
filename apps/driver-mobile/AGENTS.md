# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This is the **driver** mobile app (`apps/driver-mobile`). Clerk sign-in is **flag-gated** (`EXPO_PUBLIC_AUTH_ENABLED`); push-token registration and EAS `projectId` are wired. See [docs/driver/DRIVER-APP.md](../../docs/driver/DRIVER-APP.md) and [docs/shared/AUTH.md](../../docs/shared/AUTH.md).
