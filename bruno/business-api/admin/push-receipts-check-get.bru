meta {
  name: Check Push Receipts (GET)
  type: http
  seq: 15
}

get {
  url: {{API_URL}}/v1/push-receipts/check
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{cron_secret}}
}

docs {
  Same handler as POST. Vercel Cron typically uses GET.
}
