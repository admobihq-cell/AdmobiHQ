meta {
  name: Update fleet
  type: http
  seq: 4
}

patch {
  url: {{API_URL}}/v1/fleet/{{resource_id}}
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{clerk_jwt}}
}

body:json {
  {
    "status": "verified"
  }
}
