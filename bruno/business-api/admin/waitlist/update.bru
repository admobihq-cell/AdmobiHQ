meta {
  name: Update waitlist
  type: http
  seq: 4
}

patch {
  url: {{API_URL}}/v1/waitlist/{{resource_id}}
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{clerk_jwt}}
}

body:json {
  {
    "source": "bruno-updated"
  }
}
