meta {
  name: Update waitlist
  type: http
  seq: 4
}

patch {
  url: {{API_URL}}/v1/waitlist/{{resource_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{clerk_jwt}}
}

body:json {
  {
    "source": "bruno-updated"
  }
}
