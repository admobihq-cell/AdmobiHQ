{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["DATABASE_URL", "PAYLOAD_DATABASE_URL", "POSTGRES_URL", "POSTGRES_PRISMA_URL", "NODE_ENV", "RESEND_API_KEY", "REDIS_URL", "ADMIN_EMAIL", "SENDER_EMAIL", "TEST_RECIPIENT_EMAIL", "PAYLOAD_SECRET", "NEXT_PUBLIC_SERVER_URL", "BLOB_READ_WRITE_TOKEN", "API_KEY_PEXELS"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "env": ["DATABASE_URL", "PAYLOAD_DATABASE_URL", "POSTGRES_URL", "POSTGRES_PRISMA_URL", "NODE_ENV", "RESEND_API_KEY", "REDIS_URL", "ADMIN_EMAIL", "SENDER_EMAIL", "TEST_RECIPIENT_EMAIL", "PAYLOAD_SECRET", "NEXT_PUBLIC_SERVER_URL", "BLOB_READ_WRITE_TOKEN", "API_KEY_PEXELS"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
